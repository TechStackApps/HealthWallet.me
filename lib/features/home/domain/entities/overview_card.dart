import 'package:flutter/material.dart';

class OverviewCard {
  final String title;
  final String count;
  final IconData icon;
  final Color iconColor;

  const OverviewCard({
    required this.title,
    required this.count,
    required this.icon,
    required this.iconColor,
  });
}
